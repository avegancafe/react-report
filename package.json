{
  "name": "react-export",
  "version": "0.0.1",
  "description": "Reporting first",
  "main": "index.js",
  "scripts": {
    "build": "rollup index.js -o dist/react-export.js -f iife",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.50.0"
  }
}
